# GLOSSARY.md — Canonical Terminology

## Purpose

`GLOSSARY.md` is the source of truth for **how terms are named and what they mean** in this brain. It exists to stop terminology drift — the same system, technology, or concept being written under different names across inputs and artifacts (e.g., "order-service" vs "Order Service API", "PostgreSQL" vs "the orders database").

Agents consult this file to use canonical names and to recognise variants. When an agent meets a term that is undefined here, or that conflicts with a canonical entry, it must **raise an ambiguity flag rather than silently pick a spelling** (see `core/AGENT-RULES.md` → Terminology consistency rule).

This file governs naming. It does not replace `core/3.ARCH-TARGET.md` (system rules) or `core/1.ARCH-CONTEXT.md` (project facts).

---

## Status convention

Reuses the `core/0.ARCH-METAMODEL.md` convention:

- **`confirmed`** — canonical name and meaning verified by an authoritative source or the architect.
- **`tentative`** — in use but not yet ratified; may change.
- **`ambiguous`** — meaning or correct form is unresolved; flagged for the architect.
- **`deprecated`** — an incorrect or superseded label; do not use (kept so old references stay traceable).

---

## How this file is maintained

- **The `synthesis` agent** (reads raw input) is the primary capture point: it adds new terms, records variants with provenance, and flags ambiguities.
- **The `component-spec` / `integration-contract` agents** add or confirm terms they coin (component and contract names).
- **The `decision-record` agent** (ADR) is the promotion authority when a term encodes a real technology/architecture choice; pure spelling/naming is confirmed by the architect.
- **When to propose promotion.** A `tentative` term is proposed for `confirmed` once it recurs in a second independent session's synthesis, or enters a proposed ADR, an ARCH-TARGET entry, or a living artifact. Proposals are batched into the `fitness-review` brain-hygiene step; promotion itself stays the architect's call.
- Immutable ADRs that used an old variant are **not** edited — the **Variants seen** column keeps them traceable to the canonical term.

---

## Canonical terms

> TODO: Populate as terms are captured. Each row carries: the **canonical** term (bold, the one spelling every artifact uses), the **variants seen** in sources (transcript spellings, diagram labels, aliases — with a note on where each was seen), a concise **definition** at the right altitude (what it is, where it sits, what it is distinct from), its **status** (per the convention above, ideally with the date and who confirmed it), and the **source / owner** (the session, document, ADR, or architect ruling the entry traces to).

| Canonical | Variants seen | Definition | Status | Source / owner |
|-----------|---------------|------------|--------|----------------|
| **Immich** | — | Self-hosted photo and video management platform; the system-of-interest of this engagement. Client-server architecture, AGPL v3. | tentative | 2026-07-11 D1 synthesis; `input/systems/immich/immich-repo-readme.md` |
| **immich-server** | "Immich Server", "the server", "the backend" | The Nest.js/Node.js backend container: serves the REST API and runs the background-job workers. Follows hexagonal architecture. | tentative | 2026-07-11 D1 synthesis; `input/systems/immich/immich-architecture-overview.md` |
| **immich-machine-learning** | "the ML service", "machine learning service" | Python/FastAPI service to which all ML operations are externalized; runs ONNX models; separately deployable or disableable. | confirmed | 2026-07-11 ADR-001 (approved); `input/systems/immich/immich-architecture-overview.md` |
| **api worker** | "api" (worker) | Worker inside `immich-server` that responds to API requests for data and files from the web and mobile clients. | confirmed | 2026-07-11 ADR-002 (approved); `input/systems/immich/immich-jobs-and-workers.md` |
| **microservices worker** | "microservices", `immich-microservices` (as the split-out container / diagram label) | Worker inside `immich-server` that handles background jobs (e.g. thumbnail generation, video encoding). Runs in the same container as the api worker by default; can be split into a separate `immich-microservices` container via env vars. | confirmed | 2026-07-11 ADR-002 (approved); `input/systems/immich/immich-jobs-and-workers.md` (see synthesis C1) |
| **split workers** | "worker split", "separate API and microservices containers" | Optional deployment configuration distributing the api and microservices workers across separate containers via `IMMICH_WORKERS_INCLUDE` / `IMMICH_WORKERS_EXCLUDE`. | confirmed | 2026-07-11 ADR-002 (approved); `input/systems/immich/immich-jobs-and-workers.md` |
| **BullMQ** | — | Library used over Redis to manage Immich's background-job queues, including job chaining. | confirmed | 2026-07-11 ADR-002 (approved); `input/systems/immich/immich-architecture-overview.md` |
| **Redis** (job-queue role) | "redis" (container) | In-memory store used via BullMQ for background-job queue management; holds queue state. | confirmed | 2026-07-11 ADR-002 (approved); `input/systems/immich/immich-architecture-overview.md` |
| **Postgres** (system-of-record role) | "postgres" (container), "the database" | Persistent data store for access/authorization, users, albums, assets, sharing, and ML model settings; Immich's system of record. | tentative | 2026-07-11 D1 synthesis; `input/systems/immich/immich-architecture-overview.md` |
| **ONNX** | — | Open model-exchange format in which all Immich ML models are stored, chosen for wide support and performance. | confirmed | 2026-07-11 ADR-001 (approved); `input/systems/immich/immich-architecture-overview.md` |
| **hexagonal architecture** (Immich usage) | "ports and adapters" | Structural principle of the `immich-server` codebase: separates technology-specific implementations (`src/repositories`) from core business logic (`src/services`). | tentative | 2026-07-11 D1 synthesis; `input/systems/immich/immich-architecture-overview.md` |
| **remote machine learning** | "remote ML", "remote ML container" | Deployment mode running `immich-machine-learning` on a separate host, reached over HTTP on port 3003; the server sends image previews to it. Offloads Smart Search and Face Detection but not Facial Recognition (server-to-database). No built-in security; sequential multi-URL processing without load balancing. | confirmed — 2026-07-11 architect batch (D2 fitness F3 + component-spec proposal; trigger: recurrence in living artifacts + ADR-004) | 2026-07-11 D2 synthesis; `input/systems/immich/immich-remote-machine-learning.md` |
| **storage template** | "storage layout" | Configurable Handlebars pattern of directory/filename variables that governs on-disk media layout; default `Year/Year-Month-Day/Filename.Extension`; rendered in the server's local timezone. | tentative | 2026-07-11 D2 synthesis; `input/systems/immich/immich-storage-template.md` |
| **Storage Template Migration** (job) | "storage template migration" | Background job that applies a changed storage template to the existing library (template changes are not retroactive on their own). | tentative | 2026-07-11 D2 synthesis; `input/systems/immich/immich-storage-template.md` |
| **hardware transcoding** | "hardware-accelerated transcoding", "GPU transcoding" | Experimental GPU video transcoding via NVENC/Quick Sync/RKMPP/VAAPI; encoding-only by default; Linux/WSL2 only. Distinct from ML hardware acceleration. | tentative | 2026-07-11 D2 synthesis; `input/systems/immich/immich-hardware-transcoding.md` |
| **ML hardware acceleration** | "hardware-accelerated ML", "GPU acceleration" (for ML) | Experimental GPU acceleration of Smart Search and Facial Recognition via ARM NN / CUDA / ROCm / OpenVINO / RKNN backends; Linux/WSL2 only. Distinct from hardware transcoding (video). | confirmed — 2026-07-11 architect batch (D2 fitness F3 + component-spec proposal; trigger: recurrence in living artifacts + ADR-004) | 2026-07-11 D2 synthesis; `input/systems/immich/immich-ml-hardware-acceleration.md` |
| **Smart Search** | "smart search" (job) | ML inference step run by `immich-machine-learning`: generates search embeddings for assets. Offloadable to a remote ML host. Runs after Thumbnail Generation in the job chain. | confirmed — 2026-07-11 architect batch (D2 fitness F3 + component-spec proposal; trigger: recurrence in living artifacts + ADR-004) | 2026-07-11 component-spec-immich-machine-learning; `input/systems/immich/immich-remote-machine-learning.md` |
| **Face Detection** | "face detection" | ML **inference** step run by `immich-machine-learning`: detects faces in image previews. Offloadable to a remote ML host. Distinct from Facial Recognition (the DB-bound clustering of stored outputs). | confirmed — 2026-07-11 architect batch (D2 fitness F3 + component-spec proposal; trigger: recurrence in living artifacts + ADR-004) | 2026-07-11 component-spec-immich-machine-learning; `input/systems/immich/immich-remote-machine-learning.md` |
| **Facial Recognition** | "facial recognition" (job) | DB-bound step that clusters previously saved model outputs stored in Postgres — a **server-to-database** operation that stays server-side and is **not** offloaded to the remote ML host. Distinct from Face Detection (the inference step). | confirmed — 2026-07-11 architect batch (D2 fitness F3 + component-spec proposal; trigger: recurrence in living artifacts + ADR-004) | 2026-07-11 component-spec-immich-machine-learning; `input/systems/immich/immich-remote-machine-learning.md` |
| **shared infrastructure** (scaling) | "shared stores", "shared Postgres/Redis/filesystem" | The single Postgres, single Redis, and identical file mounts that all parallel `immich-server` instances must connect to — the precondition for horizontal scaling. All Immich state lives in these three stores. | tentative | 2026-07-11 D3 synthesis; `input/systems/immich/immich-scaling-guide.md` |
| **MODEL_TTL** | `MACHINE_LEARNING_MODEL_TTL` | Env var: idle seconds before a loaded ML model is unloaded from the `immich-machine-learning` cache; default **300**. A memory-vs-warm-start capacity lever. | tentative | 2026-07-11 D3 synthesis; `input/systems/immich/immich-environment-variables.md` |
| **buffalo_l / buffalo_s** | "buffalo models", "facial recognition model" | The two selectable facial-recognition models; `buffalo_l` is the default, `buffalo_s` the lighter alternative traded in to reduce recognition load. | tentative | 2026-07-11 D3 synthesis; `input/systems/immich/immich-faq-scaling-excerpts.md` |
| **immich-charts** (Helm chart) | "official Helm chart", "Kubernetes chart" | The official Helm chart repository — the only supported route for deploying Immich on Kubernetes. Carries no sizing guidance. | tentative | 2026-07-11 D3 synthesis; `input/systems/immich/immich-kubernetes.md` |
| **dedicated ML tier** | "ML tier", "remote-ML tier" | Operator-designed scaling unit: N stock `immich-machine-learning` containers behind an operator-run, health-checked load balancer, reached by the server as a single ML URL via an auth+TLS reverse proxy fronting port 3003. Operator-side design — the LB/proxy are not upstream-documented components. | tentative | 2026-07-11 ADR-004 (proposed) |
| **per-instance store HA** | "per-instance HA Postgres/Redis" | Recommended pilot posture: each managed instance runs its own operator-managed HA Postgres and Redis (generic replication + failover patterns), endpoint-transparent to stock containers; distinct from shared cross-instance store pools (rejected for blast radius). Operator-side design — no upstream HA path exists. | tentative | 2026-07-11 ADR-003 (proposed) |
| **per-instance media volume** | "dedicated media volume" | Recommended pilot posture: one dedicated media volume per instance on a supported (non-NTFS/FAT) filesystem holding `UPLOAD_LOCATION`, backed up operator-side via snapshot + off-instance replication in DB-first-then-filesystem order. | tentative | 2026-07-11 ADR-005 (proposed) |
| **Per-instance store HA** (enabler E-01) | "per-instance HA Postgres/Redis" | Enabler E-01: the operator capability delivering per-instance HA Postgres/Redis for NFR-001; the transition-level name for the design term *per-instance store HA*. Built in M1/WS1. | tentative | 2026-07-11 M1 transition; `core/transitions/ENABLER-CATALOG.md` (E-01); ADR-003 (proposed) |
| **Hardened scalable ML tier** (enabler E-02) | "ML tier enabler", "hardened ML tier" | Enabler E-02: the operator capability delivering a load-balanced, auth+TLS-hardened, benchmark-sized *dedicated ML tier* for NFR-003/004/006. Built in M1/WS2. | tentative | 2026-07-11 M1 transition; `core/transitions/ENABLER-CATALOG.md` (E-02); ADR-004 (proposed) |
| **Media storage & backup capability** (enabler E-03) | "media backup enabler" | Enabler E-03: the operator capability delivering *per-instance media volumes* plus operator snapshot/replication backup (DB-first-then-filesystem) and Redis durability for NFR-005. Built in M1/WS3. | tentative | 2026-07-11 M1 transition; `core/transitions/ENABLER-CATALOG.md` (E-03); ADR-005 (proposed) |
| **Bounded-window upgrade procedure** (enabler E-04) | "upgrade procedure enabler", "15-min upgrade procedure" | Enabler E-04: the operator capability delivering a rehearsed maintenance-window upgrade (server + startup migrations + version-aligned ML restart) within NFR-002's ≤15-min window while preserving the stock upgrade path (CON-001). Built in M1/WS4. | tentative | 2026-07-11 M1 transition; `core/transitions/ENABLER-CATALOG.md` (E-04); R-05 |
| **Fleet observability & health** (enabler E-05) | "fleet observability", "observability plane", "operator monitoring capability" | Enabler E-05: the operator capability delivering a central observability plane over the 50 stock instances — fleet-wide-enabled Prometheus-compatible scrape (8081/8082), central log shipping + retention of the JSON stream, operator-built health/readiness probes + container healthchecks over the stock `.immich` startup gate, and NFR-001-error-budget-tied alerting, with per-instance data segregated (ADR-003). Built in M1/WS5. Distinct from the stock primitives it wraps (*Immich telemetry*, *structured JSON logging*, *startup folder integrity check*). | tentative | 2026-07-12 M1 transition; `core/transitions/ENABLER-CATALOG.md` (E-05); ADR-007 (proposed) |
| **OIDC** (Immich usage) | "OpenID Connect", "OAuth", "OAuth/OIDC" | OpenID Connect built on OAuth2 — Immich's documented third-party authentication mechanism (Authorization Code grant, confidential web client). Configured per instance (issuer/client/scope/RS256). Distinct from local password authentication. | tentative | 2026-07-12 D4 synthesis; ADR-006 (proposed); `input/systems/immich/immich-oauth-authentication.md` |
| **OIDC claim mappings** | "claim options", "Role Claim / Storage Quota Claim / Storage Label Claim" | The three configurable IdP-claim-to-Immich-attribute mappings — Role Claim (`immich_role`), Storage Quota Claim (`immich_quota`), Storage Label Claim (`preferred_username`) — applied **only at user creation, never re-synchronized** afterward. | tentative | 2026-07-12 D4 synthesis; ADR-006 (proposed); `input/systems/immich/immich-oauth-authentication.md` |
| **immich-admin CLI** | "Server CLI", "server commands", "immich-admin" | Administrative CLI bundled in the `immich-server` image, run by connecting to the `immich_server` container. Covers admin password reset, password/OAuth login toggles, admin grant/revoke by email, maintenance mode, user listing, schema check. The operator break-glass surface. | tentative | 2026-07-12 D4 synthesis; ADR-006 (proposed); `input/systems/immich/immich-server-commands.md` |
| **break-glass** (immich-admin) | "emergency admin recovery", "lockout recovery" | The operator recovery path via the `immich-admin CLI` (container exec) — notably the only documented recovery from a both-auth-methods-disabled lockout (`immich-admin enable-password-login`) and from a lost admin password (`reset-admin-password`). | tentative | 2026-07-12 D4 synthesis; ADR-006 (proposed, retained-password-fallback posture); `input/systems/immich/immich-system-settings-auth.md`, `immich-server-commands.md` |
| **two-tier admin/user model** | "admin separation", "admin role" | Immich's documented flat authorization model — admin vs regular user, no finer-grained RBAC. First registered user becomes admin; admin membership toggled by email via the `immich-admin CLI`. | tentative | 2026-07-12 D4 synthesis; `input/systems/immich/immich-user-management.md` |
| **per-user storage quota** | "storage quota", "quota (GiB)" | Per-user upload limit in GiB (default unlimited); blocks upload once reached but exempts external libraries — an upload gate, not a hard footprint cap. Settable in the edit-user UI or via the `immich_quota` OIDC claim at creation. | tentative | 2026-07-12 D4 synthesis; `input/systems/immich/immich-user-management.md` |
| **Immich telemetry** (metric groups) | "telemetry groups", `IMMICH_TELEMETRY_INCLUDE` | Opt-in Prometheus/OpenTelemetry metrics, disabled by default; three independently toggleable metric groups — **api** (API requests), **host** (memory/CPU), **io** (DB query timings, image processing) — exposed on port 8081 (API) / 8082 (microservices). A scrape surface, not a monitoring system (no alerting shipped). | tentative | 2026-07-12 D5 synthesis; ADR-007 (proposed); `input/systems/immich/immich-monitoring.md` |
| **structured JSON logging** (Immich) | `IMMICH_LOG_FORMAT=json`, "json log format" | Env-var-selected JSON log output (records: `level`, `pid`, `timestamp` Unix-ms, `message`, `context`); verbosity via `IMMICH_LOG_LEVEL`. Configurable in **format and level only** — no first-party logging page (404), no documented routing/aggregation/retention. | tentative | 2026-07-12 D5 synthesis; ADR-007 (proposed); `input/systems/immich/immich-monitoring.md` |
| **front-door reverse proxy** (Immich) | "reverse proxy", "the proxy", "front-door proxy" | The operator reverse proxy in front of `immich-server`'s API path; must forward `Host`/`X-Real-IP`/`X-Forwarded-*`, serve on a (sub)domain root (no sub-path), keep `/.well-known/immich` routed, allow 50000M uploads with buffering off and 600s timeouts. Distinct from the ML-tier auth+TLS proxy (port 3003, ADR-004). | tentative | 2026-07-12 D5 synthesis; ADR-007 (proposed, monitored-surface + threshold); `input/systems/immich/immich-reverse-proxy.md` |
| **startup folder integrity check** (`.immich` marker) | "system integrity check", "marker-file check", "folder checks" | Startup validation that creates/reads/overwrites a hidden `.immich` marker across the six upload folders (`upload`/`library`/`thumbs`/`encoded-video`/`profile`/`backups`), catching permission and mount errors; silenceable via `IMMICH_IGNORE_MOUNT_CHECK_ERRORS`. Immich's only first-party health mechanism — a one-shot startup gate, not an ongoing container healthcheck. | tentative | 2026-07-12 D5 synthesis; ADR-007 (proposed); `input/systems/immich/immich-system-integrity.md` |

---

## Ambiguous / to-confirm

> TODO: Populate as ambiguities are flagged. Each row carries: the **term as seen** (the exact wording from the source), the **likely meaning** — including what conflicts (two candidate meanings, two spellings for one thing, or an unknown expansion) and what would resolve it (who to ask, which session or document would settle it), its **status** (`ambiguous — confirm with client`, or `resolved` with a pointer to the canonical row it became), and the **source** where the term appeared.

| Term as seen | Likely meaning | Status | Source |
|--------------|----------------|--------|--------|

---

## Deprecated / incorrect labels

> TODO: Populate as incorrect or superseded labels are ruled on. Each row carries: the deprecated **label**, the canonical term to **use instead**, and the **reason** it is wrong or superseded (mislabelled diagram, transcript spelling, an option evaluated but not selected).

| Label | Use instead | Reason |
|-------|-------------|--------|
